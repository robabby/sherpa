# Dispatch Center Research

## Iterations

1. **[Iteration 1](iteration-1.md)** (2026-03-13) — CLI flags for all 4 backends + free model benchmarks for non-coding tasks

## Open Questions

1. Exact OpenCode model IDs — need to run `opencode models opencode` locally
2. OpenCode free tier rate limits — no docs, needs empirical testing
3. Codex: target Rust CLI or TypeScript CLI?
4. Gemini model quality for content tasks vs MiniMax M2.5
5. Embedding model availability across free backends
